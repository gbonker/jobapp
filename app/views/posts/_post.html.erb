<div class="post">

  <h2><%= link_to( post.title, post, :target => '_blank' ) %></h2>
  <h6>Posted on <%= post.created_at.strftime("%A, %B %-d, %Y") %> by <%= post.user.name %></h6>
  <p><%= simple_format(post.description) %></p>
<br></br>

</div>

